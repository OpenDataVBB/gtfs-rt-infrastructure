# {{ ansible_managed }}

[Unit]
Description=restarts gtfs-rt-feed.service on changes to its pgdatabase.conf config file.

[Path]
PathChanged=/etc/systemd/system/gtfs-rt-feed.service.d/pgdatabase.conf
Unit=gtfs-rt-feed-restarter.service

[Install]
WantedBy=multi-user.target
